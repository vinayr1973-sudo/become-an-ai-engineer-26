# Phase 0 — Build Correct Mental Models (Weeks 1-2)

**Do not write a single line of agent code yet.**

Read `SKILL.md` → *Phase 0*. By the end you should be able to explain, in your
own words and without looking:

- Workflow vs Agent (and why building an agent when you need a workflow costs 10×)
- The 5 workflow patterns (chaining, routing, parallelisation, orchestrator-worker, evaluator-optimiser)
- The 4 context primitives (Write, Select, Compress, Isolate)
- The harness stack (model = CPU, harness = OS)

**Deliverable:** the 2-page concept doc. Use [`project/TEMPLATE.md`](project/TEMPLATE.md).
If you can't write it without looking, you haven't understood it yet.
